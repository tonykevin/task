/* import {
 *   GET_USER,
 *   LOGIN_ERROR,
 *   LOGIN_SUCCESSFUL,
 *   LOGOUT,
 *   SIGNUP_ERROR,
 *   SIGNUP_SUCCESSFUL
 * } from '../../types' */

export default (state, action) => {
  switch (action.type) {
    default:
      return state
  }
}
